version: "3.1"

rules:
  # - rule: Say goodbye anytime the user says goodbye
  #   steps:
  #   - intent: goodbye
  #   - action: utter_goodbye

  - rule: hello world
    steps:
      - intent: hello_world
      - action: action_hello_world

  - rule: allow
    steps:
      - intent: allow
      - action: action_send_intent

  - rule: deny
    steps:
      - intent: deny
      - action: action_send_intent
